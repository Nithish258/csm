rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Temporarily allowing all reads/writes for authenticated users
      // to debug connectivity issues without rule interference.
      allow read, write: if request.auth != null;
    }
  }
}
